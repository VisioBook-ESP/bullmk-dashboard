{{- if and .Values.istio .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "bullmq-dashboard.fullname" . }}
  labels:
    {{- include "bullmq-dashboard.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "bullmq-dashboard.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "bullmq-dashboard.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}
