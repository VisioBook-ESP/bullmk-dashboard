apiVersion: v1
kind: Service
metadata:
  name: {{ include "bullmq-dashboard.fullname" . }}
  labels:
    app: {{ include "bullmq-dashboard.name" . }}
    version: {{ .Values.image.tag | quote }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "bullmq-dashboard.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      {{- if eq .Values.service.type "NodePort" }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
